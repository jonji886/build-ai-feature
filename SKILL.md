---
name: build-ai-feature
description: 从需求到已验证交付，构建或实质性变更一个 AI 产品功能。适用于 RAG、意图路由、记忆、Agent、场景技能（Scenario Skill）、Tool 工作流、人工交接、安全控制等需要界定需求、架构、测试、评估与同步文档的 AI 能力。也用于将 AI 原型产品化，或审计某个 AI 功能是否真正可交付。不用于纯评估类工作或琐碎的孤立修改。
---

# 构建 AI 功能

将快速的 AI 辅助探索转化为有证据支撑的交付。把代码、契约、测试、评估与决策文档视为一次统一的变更。

## 选择运行模式

- 对于构建或变更请求：检视、设计、实现、验证并编写文档。
- 对于审计或评审请求：检视并报告证据；除非用户要求变更，否则不要改动。
- 对于纯评估类工作，如宿主已安装 `evaluate-ai-feature`，则切换到该 Skill。

## 保持宿主中立

把本文件作为唯一的工作流事实来源。不要假设某个 Agent 专属的命令、工具名或目录结构；先使用当前宿主实际提供的文件、终端、搜索和测试能力，再按本工作流完成任务。所有相对资源路径都从本 Skill 根目录解析。

只有在安装、迁移或排查发现问题时，才阅读 `references/agent-compatibility.md`。`agents/openai.yaml` 是可选的 OpenAI 展示元数据，不是执行本 Skill 的前提。

## 执行工作流

### 1. 发现事实来源

阅读项目说明以及最小相关集合的需求、架构、决策记录、代码、测试与评估产物。遵守项目的冲突解决顺序。保留用户与本次无关的变更。

进入不熟悉的代码仓库时，从本 Skill 根目录运行 `python3 scripts/audit_ai_delivery.py <项目>` 以获得快速的建议性盘点。将其发现视为检视的提示，而非质量证明。

### 2. 界定产品变更

说明：

- 目标用户与问题；
- 期望行为与可度量的结果；
- 范围内与范围外的行为；
- 输入、输出、依赖、权限与失败模式；
- 可执行或可观察的验收标准。

不要让实现悄然成为隐含需求。对于重大或模糊的变更，在编码之前先更新或提出规格说明。

### 3. 将每个决策放入正确的能力层

在决定行为应归属于提示词（Prompt）、目录/配置、Agent、场景技能（Scenario Skill）、Tool、记忆存储还是评估器（evaluator）时，阅读 `references/ai-capability-patterns.md`。

将业务事实与写入操作放在确定性服务或 Tool 后面。不要仅在 Prompt 中实现授权、确认、租户隔离或数据校验。将频繁变更、被审计、需独立评估的业务边界做成带版本号的资产。

### 4. 定义风险与跨层不变量

对于高风险、有状态或多步骤功能，阅读 `references/risk-and-invariants.md`。

在实现之前先写下不变量。表达完整的语义链，例如：

```text
Tool 返回 requires_human
→ 场景状态为 handoff（人工交接）
→ 对话保持未解决
→ 自动化指标排除该案例
→ 追踪记录解释该决策
```

对授权绕过、跨用户泄漏、未确认写入、重复写入、过期策略泄漏或漏掉关键风险路由，使用零容忍发布门禁。不要把这些失败平均进一个笼统的准确率分数里。

### 5. 复杂实现前先设计

对于复杂或高风险变更，记录：

- 模块职责与数据流；
- API、Tool、状态与错误契约；
- 权限与确认边界；
- 超时、重试、降级与交接行为；
- 可观测性与版本字段；
- 备选方案、决策、理由与权衡。

仅当项目缺少自己的 ADR 约定时才使用 `assets/adr-template.md`。琐碎变更不要写架构文档。

### 6. 用基于风险的测试实现

先测试代价最高的失败。优先覆盖权限、隔离、状态转换、业务写入、降级与外部依赖失败，而不是表面的覆盖率。

实现最小且连贯的变更。复用现有服务与约定。避免无关的重构，也不要为了"通过"而削弱断言。

### 7. 在决策边界上评估

不要只依赖最终答案的测试。在适用处，将路由、执行、事实、安全、用户结果与系统行为分开评估。如宿主已安装 `evaluate-ai-feature`，可用它完成专项评估设计。

至少证明：

- 功能选择了正确的路径；
- 所选路径执行了它的契约；
- 确定性的事实与权限保持正确；
- 受控失败能安全降级；
- 最终用户可见的结果与业务状态一致。

### 8. 同步决策与证据

阅读 `references/documentation-and-delivery.md`。只更新受影响的文档，但必须记录重大的产品决策，包含：

- 为什么选择这个设计；
- 考虑过的备选方案；
- 没有它会怎样；
- 权衡与当前限制；
- 该决策如何被评估。

尽可能从可执行的结果生成报告。不要在多份文档中各自维护同一个指标。

### 9. 验证与交接

运行项目相关的格式化、lint、类型检查、单元、集成、评估、构建与场景测试。在最终行为变更后重新生成报告。检查是否有过期的指标和意外产生的文件。

报告：

- 交付的行为；
- 重要的设计决策；
- 实际变更的文件；
- 实际运行的命令及其结果；
- 已知限制与生产差距。

当有必要的门禁被跳过或失败时，绝不宣称完成。

## 可复用资源

- `references/agent-compatibility.md`：跨 Agent 的安装、调用与兼容边界。
- `references/ai-capability-patterns.md`：选择正确的 AI 能力边界。
- `references/risk-and-invariants.md`：对风险分类并定义跨层不变量。
- `references/documentation-and-delivery.md`：同步产品决策与交付证据。
- `scripts/audit_ai_delivery.py`：盘点常见的交付产物与变更文件覆盖情况。
- `scripts/manage_skill.py`：安装到常见 Agent，或生成可移植 ZIP 包。
- `assets/adr-template.md`：极简决策记录模板。
- `assets/acceptance-criteria-template.md`：可执行的验收标准模板。
